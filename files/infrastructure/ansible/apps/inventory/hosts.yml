all:
  children:
    apps:
      hosts:
        apps-vm:
          # Phase 1 management address. Confirm the address and VMID are free
          # on the live network before the first Terraform apply.
          ansible_host: 192.168.10.42
          ansible_user: deploy
          ansible_become: true
          ansible_python_interpreter: /usr/bin/python3
