apiVersion: v1
kind: ConfigMap
metadata:
  name: stashpad-config
  labels:
    app.kubernetes.io/name: stashpad
data:
  # ホスト側の実体: NFS 192.168.10.11:/mnt/shared/koji-genba/stashPadLib
  # (overlays/*/pv-media.yaml でこのパスをまるごと /media に読み取り専用マウントしている)
  library-roots: "/media"
