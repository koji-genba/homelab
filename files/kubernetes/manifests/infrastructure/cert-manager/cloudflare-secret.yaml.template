# Cloudflare API Token Secret Template
# 使用方法:
# 1. このファイルをコピー: cp cloudflare-secret.yaml.template cloudflare-secret.yaml
# 2. YOUR_CLOUDFLARE_API_TOKEN_HERE を実際のTokenに置換
# 3. kubectl apply -f cloudflare-secret.yaml
# 4. cloudflare-secret.yaml を削除

apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-api-token-secret
  namespace: cert-manager
type: Opaque
stringData:
  api-token: "YOUR_CLOUDFLARE_API_TOKEN_HERE"